let routerNum = 10;

for (ttl = 1; routerNum > ttl; ttl++){
    for(routerNum = 1; routerNum != ttl; routerNum++){

        console.log("the Time To Live is " + ttl + 
        ", which has linked to the router number " + routerNum);
        
        console.log("/n");
    }    
}
